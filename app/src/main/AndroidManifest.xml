<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shrikant.com.quizapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartupPage"
            android:theme="@style/Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin.Dashboard" />
        <activity android:name=".Admin.LoginPage" />
        <activity android:name=".Admin.MultichoiceQuestion" />
        <activity android:name=".Admin.Descriptive_Question" />
        <activity android:name=".Admin.ViewQuestions" />
        <activity android:name=".Admin.Registration" />
        <activity android:name=".Student.ViewDescQuestion" />
        <activity android:name=".Student.StudentDahboard" />
        <activity android:name=".Student.ViewMultiQuestions" />
        <activity android:name=".Student.Profile"></activity>
    </application>

</manifest>